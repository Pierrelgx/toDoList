import type { DefaultTheme } from "styled-components";

const colors = {
  mexicanRed: "#A4262A",
};

const fontWeights = {
  bold: "700",
  regular: "400",
};

export const theme: DefaultTheme = {
  colors,
  fontWeights,
};